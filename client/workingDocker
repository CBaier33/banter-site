# Build stage
FROM node:22 AS build
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install --legacy-peer-deps
COPY . .
RUN npm run build --prod

# Serve stage
FROM caddy:2
COPY --from=build /app/dist/banter-site /usr/share/caddy
COPY Caddyfile /etc/caddy/Caddyfile

